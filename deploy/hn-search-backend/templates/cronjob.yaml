apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ .Release.Name }}-daily-ingest
  labels:
    app: {{ .Chart.Name }}
spec:
  schedule: "0 6 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      activeDeadlineSeconds: 3600
      template:
        spec:
          imagePullSecrets:
            - name: ghcr
          containers:
            - name: ingest
              image: "curlimages/curl:latest"
              command:
                - /bin/sh
                - -c
                - |
                  curl -f -X POST http://{{ .Release.Name }}-service:{{ .Values.service.port }}/api/ingest/daily
              resources:
                limits:
                  cpu: '100m'
                  memory: '64Mi'
          restartPolicy: OnFailure
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ .Release.Name }}-prune
  labels:
    app: {{ .Chart.Name }}
spec:
  # Runs after the daily ingest so a fresh fetch is never pruned in the same pass.
  schedule: {{ .Values.retention.schedule | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      activeDeadlineSeconds: 900
      template:
        spec:
          imagePullSecrets:
            - name: ghcr
          containers:
            - name: prune
              image: "curlimages/curl:latest"
              command:
                - /bin/sh
                - -c
                - |
                  curl -f -X POST http://{{ .Release.Name }}-service:{{ .Values.service.port }}/api/ingest/prune
              resources:
                limits:
                  cpu: '100m'
                  memory: '64Mi'
          restartPolicy: OnFailure
